import { createAction, props } from '@ngrx/store';

export const toggleZoom = createAction('[Ui] Toggle Zoom');

export const enableZoom = createAction('[Ui] Enable Zoom');

export const disableZoom = createAction('[Ui] Disable Zoom');
